#include <stdio.h>
int circle_area(double radius, double radius)
{
	double result;
	result = radius * radius * 3.14;
	return result;
}
int main(void)


{
	double result;
	double radius;
	scanf("%lf", &radius);
	result = cir;
	printf("%lf", result);
}