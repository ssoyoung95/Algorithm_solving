// http://www.jungol.co.kr/bbs/board.php?bo_table=pbank&wr_id=259&sca=10h0

#include <stdio.h>
int main(void)
{
   int array[5]; // 5개짜리 정수형 배열 선언
   int idx ; // 입력값을 받기위한 변수값
   int* p = //array[5]? array? ; //포인터 변수에 저장하기 위한 형태?
   
   for( idx=0 ; idx<5 ; idx++)//5개 입력받기
   {
      scanf("%d", //&p[idx] p[idx]); // ??
   }
   for (idx = 0; idx < 5; idx+=2)//홀수번째 출력
   {
      scanf("%d", //&p[idx] p[idx]); // ??
   }

   return 0;
}